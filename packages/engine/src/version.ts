/**
 * Engine version, stamped into every {@link CoordinationResult} and written to
 * project files. Bump it whenever a change can alter the plan produced for an
 * unchanged input — golden files in `docs/VALIDATION.md` are keyed to it.
 */
export const ENGINE_VERSION = '0.1.0';
